# Continuation Prompt

Use this when resuming work in a new Claude Code session.

---

I'm resuming work on fabled10x. Before we do anything:

1. Read `pipeline/active/session.yaml` to see the current session state and what's been completed
2. Read `pipeline/active/knowledge.yaml` for cross-section context
3. Read the README of the currently-active job in `currentwork/{current_phase}/README.md`
4. If there's an active section, read everything in `pipeline/active/{current_section}/`

Then tell me what's next.
